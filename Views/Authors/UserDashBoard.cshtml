
@{
    ViewBag.Title = "UserDashBoard";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<fieldset>
    <legend> User DashBoard </legend>

    @if (Session["Fname"] != null)
    {
        <text>
            Welcome @Session["Fname"].ToString()  @Session["ProfileImage"].ToString()
        </text>
    }
</fieldset>





